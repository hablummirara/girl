---
const navItems = [
  { href: "/", label: "Home" },
  { href: "/products", label: "Products" },
  { href: "/about", label: "About" },
  { href: "/faq", label: "FAQ" },
];

const path = Astro.url.pathname.replace(/\/$/, "") || "/";
---

<header class="site-header">
  <nav class="nav container" aria-label="Navigasi utama">
    <a class="brand" href="/">
      <span class="brand-mark" aria-hidden="true">
        <span class="brand-ring"></span>
        <span class="brand-initials">GC</span>
        <span class="brand-dot dot-one"></span>
        <span class="brand-dot dot-two"></span>
      </span>
      <span>Girl Collection</span>
    </a>

    <div class="nav-links" data-nav-links>
      {navItems.map((item) => (
        <a class:list={["nav-link", path === item.href && "active"]} href={item.href}>{item.label}</a>
      ))}
    </div>

    <div class="nav-actions">
      <a class="icon-link" href="/products" aria-label="Cari produk">
        <span class="material-symbols-outlined">search</span>
      </a>
      <a class="cart-link" href="/cart" aria-label="Keranjang belanja">
        <span class="material-symbols-outlined">shopping_cart</span>
        <span data-cart-count class="is-empty">0</span>
      </a>
      <button class="menu-toggle" type="button" aria-label="Buka menu" aria-expanded="false" data-menu-toggle>
        <span class="material-symbols-outlined">menu</span>
      </button>
    </div>
  </nav>
</header>

<script>
  const toggle = document.querySelector("[data-menu-toggle]");
  const links = document.querySelector("[data-nav-links]");

  toggle?.addEventListener("click", () => {
    const isOpen = toggle.getAttribute("aria-expanded") === "true";
    toggle.setAttribute("aria-expanded", String(!isOpen));
    links?.classList.toggle("open", !isOpen);
    document.body.classList.toggle("menu-open", !isOpen);
  });
</script>

<style>
  .site-header {
    position: fixed;
    inset: 0 0 auto;
    z-index: 20;
    border-bottom: 1px solid rgba(220, 191, 198, 0.55);
    background: rgba(255, 248, 248, 0.84);
    backdrop-filter: blur(16px);
    box-shadow: 0 18px 46px -38px rgba(170, 44, 98, 0.8);
  }

  .nav {
    min-height: 78px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 24px;
  }

  .brand {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    color: var(--primary);
    font-family: "Plus Jakarta Sans", sans-serif;
    font-size: 1.2rem;
    font-weight: 800;
  }

  .brand-mark {
    position: relative;
    display: grid;
    width: 50px;
    height: 50px;
    flex: 0 0 50px;
    place-items: center;
    overflow: hidden;
    border: 1px solid rgba(170, 44, 98, 0.18);
    border-radius: 18px;
    background:
      radial-gradient(circle at 22% 20%, rgba(255, 255, 255, 0.95) 0 14%, transparent 15%),
      linear-gradient(135deg, #ff8fbd 0%, var(--primary-soft) 42%, var(--secondary-soft) 100%);
    box-shadow:
      inset 0 0 0 1px rgba(255, 255, 255, 0.62),
      0 14px 30px -18px rgba(170, 44, 98, 0.9);
  }

  .brand-mark::before,
  .brand-mark::after {
    content: "";
    position: absolute;
    border-radius: 999px;
    pointer-events: none;
  }

  .brand-mark::before {
    width: 56px;
    height: 56px;
    right: -28px;
    bottom: -24px;
    background: rgba(170, 44, 98, 0.18);
  }

  .brand-mark::after {
    width: 18px;
    height: 18px;
    left: 7px;
    bottom: 8px;
    background: rgba(255, 255, 255, 0.5);
  }

  .brand-ring {
    position: absolute;
    inset: 7px;
    border: 1px solid rgba(255, 255, 255, 0.72);
    border-radius: 14px;
    transform: rotate(8deg);
  }

  .brand-initials {
    position: relative;
    z-index: 1;
    color: var(--primary-strong);
    font-size: 1.08rem;
    font-weight: 900;
    line-height: 1;
    text-shadow: 0 1px 0 rgba(255, 255, 255, 0.55);
  }

  .brand-dot {
    position: absolute;
    z-index: 1;
    display: block;
    border-radius: 999px;
    background: white;
    box-shadow: 0 0 0 3px rgba(255, 255, 255, 0.28);
  }

  .dot-one {
    width: 5px;
    height: 5px;
    top: 10px;
    right: 11px;
  }

  .dot-two {
    width: 4px;
    height: 4px;
    right: 16px;
    bottom: 12px;
  }

  .nav-links {
    display: flex;
    align-items: center;
    gap: 28px;
  }

  .nav-link {
    color: var(--muted);
    font-family: "Plus Jakarta Sans", sans-serif;
    font-size: 0.88rem;
    font-weight: 800;
  }

  .nav-link.active,
  .nav-link:hover {
    color: var(--primary);
  }

  .nav-actions {
    display: inline-flex;
    align-items: center;
    gap: 12px;
  }

  .icon-link,
  .cart-link,
  .menu-toggle {
    display: grid;
    width: 42px;
    height: 42px;
    place-items: center;
    border: 0;
    border-radius: 999px;
    background: transparent;
    color: var(--primary);
    cursor: pointer;
  }

  .cart-link {
    position: relative;
  }

  .cart-link > span:last-child {
    position: absolute;
    top: 0;
    right: 0;
    display: grid;
    width: 19px;
    height: 19px;
    place-items: center;
    border: 2px solid var(--surface);
    border-radius: 50%;
    background: var(--primary-bright);
    color: white;
    font-size: 0.66rem;
    font-weight: 800;
  }

  .cart-link > span:last-child.is-empty {
    display: none;
  }

  .menu-toggle {
    display: none;
  }

  @media (max-width: 760px) {
    .nav {
      min-height: 68px;
    }

    .brand span:last-child {
      display: none;
    }

    .menu-toggle {
      display: grid;
    }

    .nav-links {
      position: fixed;
      inset: 68px 0 auto;
      display: none;
      flex-direction: column;
      align-items: stretch;
      gap: 0;
      border-bottom: 1px solid var(--outline);
      background: var(--surface);
      padding: 12px 20px 20px;
    }

    .nav-links.open {
      display: flex;
    }

    .nav-link {
      padding: 14px 4px;
    }
  }
</style>
